{
  "name": "OpenApiSpec-Hono",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.3",
    "@hono/zod-openapi": "^1.2.0",
    "hono": "^4.11.4"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
